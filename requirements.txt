# Runtime dependencies for Fast Todo app
fastapi
uvicorn[standard]
sqlmodel
SQLAlchemy
aiosqlite
pydantic
starlette
jinja2
python-multipart

# Dates and recurrence parsing
python-dateutil
dateparser

# Auth & crypto
passlib[bcrypt]
python-jose[cryptography]
cryptography

# HTTP clients (used by server utilities, scripts, and tests)
httpx
requests

# Optional services used by features/scripts
#asyncssh
#beautifulsoup4
#pywebpush
